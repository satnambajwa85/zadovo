Instruction to use the span plugin

1) In config.js file. Add the below attribute.

config.extraPlugins = 'span';

2) Copy and paste the span folder inside the plugins folder in ckeditor.

3) use toolbar : [ 'Span' ]  or Span tag to show the span button in your editor toolbar